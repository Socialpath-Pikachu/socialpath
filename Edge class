package Socialpath;

public class Edge {
    Vertex toVertex;
    double fromVertexRep;
    Edge nextEdge;
	
    public Edge(){
	toVertex = null;
	fromVertexRep = 0;
	nextEdge = null;
    }
	
    public Edge(Vertex destination, double rep, Edge a){
	toVertex = destination;
        fromVertexRep = rep;
	nextEdge = a;
    }
}
